# Exact candidate Linux validation

This kit reconstructs preview candidate d99c554b7de27754609d6a7b731afc50f01c224854ab176985440af0e999bb1f.
The transport manifest uses the earlier cain-candidate/1 identity algorithm.
All overlaid runtime bytes are independently hashed before Linux execution.
Unrecorded working-tree changes fail validation. Full runtime overlays preserve
Windows-captured bytes across checkout line-ending policies.

Publication of this validation branch and Actions was explicitly authorized by
the user when requesting completion of the remaining gates. This does not authorize
merge, release, active installation or scientific execution. No real corpus is uploaded.
Prepared scripts and a pushed branch are not evidence of a successful Linux run.
