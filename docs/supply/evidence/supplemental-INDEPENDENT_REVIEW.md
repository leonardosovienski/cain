# Independent review — supplemental real documentation derivation

Supplemental identity: 3f3b34cf116201aea601459668b70480570fa9583575e727228e75fd0c2a8063. Runtime remains 7da108d0172b7ccc2675404b7dbf0b8a21ad248ee814ea14f199225003d23861, based on delivery d00bbc6fb910b3c51be6720d21012f4655258468adb7d7335451974929c58752.

**Conclusion: claims of bounded structural equality (NEUTRAL), process persistence, offline restoration and generation revocation are supported for the three new local documentation publications. No reproducible functional defect was identified in this supplemental review.** This conclusion does not certify positive benefit, generalization, semantic model quality, a new Linux corpus run, or human freeze approval.

## Independently checked evidence

- Recomputed supplemental identity and hashes of harness, authorization, protocol and staging policy. Verified installed code identity remains unchanged from the prior delivery.
- Original delivery policy still matches its frozen hash. For Core/Ops/Ecosystem, original manifest input hashes equal new publication input hashes and the authorization's source hashes. Current source-file bytes and every received published object match those hashes. Each new bundle has a distinct ID, permits local generation and forbids disclosure.
- Core contains 16 document entities / 16 relations / 16 received objects; Ops 11/11/11; Ecosystem 6/6/6; reference-only counts are zero. Independently counted new validated manifests equal protocol expectations and diagnostic assertions. This is the precise basis of NEUTRAL; it is equality of recovered counts, not a measured productivity or reasoning improvement.
- Read 47 public CLI receipts. Creation and subsequent diagnostics reads use different subprocesses. Before offline backup, import roots are made unavailable and derivations are recreated under that exact policy; restored diagnostics equal those recorded rows. This proves the exercised persistent snapshot survives offline backup/restore without rereading producer roots.
- All three revoked reads failed specifically with NOT_AUTHORIZED_FOR_DERIVATION. The paired factual queries succeeded. The harness only asserts exit 1 for denial, but inspection of actual receipts rules out an unrelated failure in this run.
- Observed supplemental directory size was 3,222,511 bytes, 47 calls and approximately 82.58 seconds, below declared numerical budgets. Receipt and identity details: SUPPLEMENTAL_REAL_IDENTITY.json.

## Scope and wording limits

AUTHORIZATION.json records the engineer's bounded interpretation of the user's contextual 'faz entao'; it is not a separately signed document containing the user's verbatim detailed scope. That interpretation is consistent with the supplied task context and new local publications of already admitted documentation. Old restrictions are not retroactively rewritten; new policy/publications must remain clearly identified as supplemental.

The reserved follow-up question in PROTOCOL.json is not actually submitted to a product question-answering path by this harness. It cannot be counted as an executed held-out question or evidence of generalization. The current results correctly disclaim learning/generalization; preserve that qualification.

The harness checks size/time between calls and output size after capture. These are observed-budget guards, not the strict streaming/output-write enforcement implemented in the separately reviewed finite runner. No budget overrun was observed here. Do not describe this supplemental script as an independently hardened resumable runner: it records a completion checkpoint after successful completion, without implementing restart from partial state.

Real inputs here mean authored documentation from three specific producers. They do not include new scientific experiments or protected/third-party datasets and do not make the other original restricted collections generation-enabled. Prior unsupported model synthesis remains a separate limitation and is not retested or repaired by document-count equality.

All review operations were read-only outside the review directory. No shared source, policy, active instance or producer was changed by this reviewer.
